mixin article(link, img, caption, small_img)
  .article
    p
      block
      |  (
      a(href=link target="_blank") источник
      | )
    if img
      figure
        img(data-src=img)
        figcaption=caption
    hr
    button.more ⌄
